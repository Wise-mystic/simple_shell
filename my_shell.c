#include "shell.h"

/*
 *this is the prompt invoker
 */

{
int main(void);

{
	char command[130];

while (true);

{
	display_prompt();
	read_command(command, sizeof(command));
	execute_command(command);
}
}
	return 0;
	}
